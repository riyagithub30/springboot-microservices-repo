spring.application.name=limit-service

#spring.config.import=optional:configserver:http://localhost:8888 --optional is used when spring-cloud-config-server is not up and running
#bootstrap.properties file is used when we want to access properties file from spring-cloud-config-server


spring.config.import=configserver:http://localhost:8888

#
#accessing application properties values directly from spring-cloud-config-server property file
#
#
